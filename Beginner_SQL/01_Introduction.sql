SELECT *
FROM parks_and_recreation.employee_salary;

SELECT first_name
FROM parks_and_recreation.employee_salary;

SELECT first_name,occupation
FROM parks_and_recreation.employee_salary;

#here you have to note that 
# -->more than 2 queries are present then running flash button will give you both the outputs
# -->when you only want one then simply hover it and use flash button +i or select it and use flash button